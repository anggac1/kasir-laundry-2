<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        CATATAN PENTING SOAL IZIN

        Aplikasi ini SENGAJA TIDAK meminta android.permission.INTERNET.
        Tanpa izin itu, sistem Android sendiri yang memblokir segala
        koneksi keluar. Semua data hanya hidup di dalam HP ini.

        Izin di bawah semuanya hanya untuk printer thermal Bluetooth.
    -->

    <!-- Android 11 (API 30) ke bawah -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!--
        Android 11 ke bawah mewajibkan izin lokasi untuk memindai Bluetooth.
        Dibatasi maxSdkVersion 30 supaya di Android 12+ TIDAK diminta sama sekali.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!--
        Android 12 (API 31) ke atas.
        neverForLocation menyatakan hasil pemindaian tidak dipakai
        untuk menebak lokasi pengguna.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-feature android:name="android.hardware.bluetooth"
        android:required="false" />

    <application
        android:label="Kasir Laundry"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>

        <!--
            Sejak Android 11, aplikasi harus menyebutkan lebih dulu jenis
            aplikasi lain yang ingin dituju. Tanpa blok ini, membuka tautan
            gagal diam-diam karena sistem berpura-pura tidak ada peramban.

            Ini BUKAN izin internet. Aplikasi hanya menyerahkan alamatnya
            ke peramban; ia sendiri tetap tidak bisa berkirim data.
        -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
