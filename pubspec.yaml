name: laundry_pos
description: Aplikasi kasir laundry offline, tanpa login, database lokal.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  shared_preferences: ^2.2.2
  print_bluetooth_thermal: ^1.1.0
  permission_handler: ^11.3.1
  # Ekspor laporan
  pdf: ^3.10.8
  excel: ^4.0.6
  path_provider: ^2.1.3
  share_plus: ^7.2.2

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
